```
[root@osboxes ~]# vi /etc/hosts
[root@osboxes ~]# vi /etc/hostname
[root@osboxes ~]# cat /etc/hosts
127.0.0.1   localhost ansible-controller
::1         localhost ansible-controller
[root@osboxes ~]# cat /etc/hostname
ansible-controller

shutdown now -r
```
